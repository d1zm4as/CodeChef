# CodeChef - BeginnerLevel

Beginner-level CodeChef solutions implemented in C.

## Organization

- One `.c` file per problem.
- File names generally match the CodeChef problem code (for example `ACTEMP.c`, `BLACKJACK.c`).

## Build and run

```bash
gcc ACTEMP.c -o run
./run
```
